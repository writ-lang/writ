; recurse.pol — a form whose template invokes itself. Form hygiene rejects it
; at collection (a template may mention only reserved words, its slots, and
; EARLIER forms), so `pol check recurse.pol` fails with a positioned
; "recurses" error before the model is ever parsed.

(form (loop X) (loop X))

(loop hi)
