# Keep the build context small and reproducible. The build stage compiles
# tooling/cli/writ.exe from core/ + runtime/ + tooling/cli/; the runtime stage
# builds writ from source; the worked examples live in a repository of their own.
_build
dist
.git
tooling/lsp
docs
scripts
tests/unit
tests/models
**/*.install
**/.DS_Store
