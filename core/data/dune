; The data model layer: errors, values, schema/instance, state, model, claims.
; A LEAF library — no dependencies at all, not even sibling writ_* libraries, so
; the OCaml stdlib is all it may use. Every other writ library is built on top of
; this one; nothing here may reach up to the front end or the interrogator.

(library
 (name writ_data))
