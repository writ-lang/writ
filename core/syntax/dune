; The front end: reader, form expander, grammar, parser, declarations, claims
; parser, loader. It turns source text into the core data model, so it depends
; on writ_data and nothing else — never the interrogator (writ_runtime).

(library
 (name writ_syntax)
 (libraries writ_data))
